# Приём шары Яндекс.Диска · 27.08.2026

Статус: **частичный intake**. Идентичность двух баз (имя+версия из XML + роль узла от заказчика) — `VERIFIED`. Данные ИБ **не** верифицированы. Payload в Git **не** кладётся. G1 **не** открывается. D-47 **сужен, не закрыт**. `dumpLagWeeks` **не** включать.

Канонический URL: https://disk.yandex.ru/d/dfKzWDC27vtTFg

Манифест: [config-dump-manifest.yandex-share.sanitized.json](config-dump-manifest.yandex-share.sanitized.json)  
`intakeId`: `a19e6c40-7b2d-4f11-8c55-2d9a0b47e801`

Это **новая** датированная папка research: полные XML конфигураций появились 27.08. Частичный Catalogs-zip 26.08 остаётся в силе; набор имён справочников Розницы совпал (735 = 735).

## 1. Что на шаре

Корень: 22 элемента, 8 каталогов интервью/ролей и 14 файлов. Новые каталоги дампов (созданы 27.08): `/Розница`, `/УТ Новая`.

Полный перечень имён: [share-inventory.json](share-inventory.json). Все **91** файл шары (~7.05 ГиБ по API) скопированы в карантин `.local/dumps/incoming/yandex-2026-08-27/` на машине агента; в Git их нет. Коммерческие прайсы, выписка и видео **не** индексируются как конфигурация 1С.

### `/Розница`

| Файл | Размер | SHA-256 | Чем является |
|---|---|---|---|
| `ConfigFiles.zip` | 2 204 363 437 | `8dba2d5703f2041c3b67ee1d22329b919761d9fb3b069272573934e49ef44a76` | Полная XML конфигурации. Подпись ZIP (`PK`). Есть `Configuration.xml` и `ConfigDumpInfo.xml`. 67 674 файлов, ~15 305 `.bsl`. Внутри ещё vendor `Ext/ParentConfigurations/Розница.cf` (~1.13 ГиБ) — не извлекался в дерево Git |
| `F.zip` | 509 062 | `b3f6a7921339c6b7e8fe7a68e9ebb575ee7b420f46e5e058cb834588a9bbb3f2` | XML расширения `СНЭКС_Доработки` |
| `F-2.zip` | 67 475 | `71d6697ecb150cb2c97e74c71be831570dea233a61c981172666d0190c16a8fe` | XML расширения `СНЭКС_ФункционалОбъединенияВидовЦенПоРегионам` |
| `Forus-1.zip` | 8 506 | `60a4c0d1ebc336b154abdaf4820bcd85b9e742dd34525671eac81badc46512d1` | XML расширения `Форус_ОснованиеТоварнойНакладнойВозврат` |
| `maxma.zip` | 334 929 | `5d56de5b8969bcba43314aeddd04052f21ba6cb1ed297b395b0391abb89d3c58` | XML расширения `maxma` (AddOn) |

### `/УТ Новая`

| Файл | Размер | SHA-256 | Чем является |
|---|---|---|---|
| `YT.zip` | 1 818 810 126 | `e25d29eef406de969b5177d365e771adfb6832dc4ea1568d01b418bf844a8863` | Полная XML конфигурации УТ. Есть `Configuration.xml`. 48 757 файлов, ~12 447 `.bsl`. Внутри vendor `Ext/ParentConfigurations/УправлениеТорговлей.cf` (~939 МиБ) |
| `YT-1.zip` | 10 713 | `1d8a0804d422de4778380f3ab7256c12cb0fb9406986bdbc656b7452dc0babe1` | XML расширения `СНЭКС_ДоработкаОбмена` |
| `YT-2.zip` | 1 715 471 | `0844fdc292eb4340d86814a498a309d9bae3b230553f8e10b25dccd6b5beb1ab` | XML расширения `СНЭКС_ЗагрузкаДанныхИзВнешнегоИсточника` |

Нет `*.dt`, нет `*.cfe` как отдельных файлов, нет выгрузки БП, нет XML/DT **данных** справочников.

## 2. Релизы из `Configuration.xml`

Имя и версия — чтение `Configuration.xml`, не догадка. Заказчик 27.08.2026 письменно подтвердил роли узлов этих двух ZIP (не гипотеза папки): `/Розница` + `ConfigFiles.zip` — центральная рабочая Розница; `/УТ Новая` + `YT.zip` — центральная УТ **пилота**. Это не объявление, что legacy УТ мертва. `platformVersion` из XML по-прежнему нет.

| База манифеста | Имя | Синоним | Версия | CompatibilityMode | Справочников | Документов | Планов обмена |
|---|---|---|---|---|---|---|---|
| `RETAIL_DUMP_01` | `Розница` | Розница, редакция 3.0 | `3.0.13.342` | `Version8_5_1` | 735 | 346 | 26 |
| `UT_DUMP_01` | `УправлениеТорговлей` | Управление торговлей, редакция 11 | `11.5.22.164` | `Version8_3_27` | 479 | 280 | 21 |

Платформа (`platformVersion`) из этих ZIP не читается. `supportMode=PARTIAL`: есть vendor parent CF и доработки в основной конфигурации.

Индекс: [retail-config-index.json](retail-config-index.json), [ut-config-index.json](ut-config-index.json). В публичном JSON нет имён и тел процедур BSL.

## 3. MDM (экземпляры по-прежнему нельзя заполнить)

| Конфигурация | `Магазины` | `СтруктурныеЕдиницы` | `Склады` | `Организации` |
|---|---|---|---|---|
| Розница 3.0.13.342 | нет | есть | нет | есть |
| УТ 11.5.22.164 | нет | нет | есть | есть |

Гипотеза «магазин в Рознице = `СтруктурныеЕдиницы`» **не снята** и **не стала VERIFIED**: нет элементов справочника, нет GUID. В УТ объекта `СтруктурныеЕдиницы` нет — складской объект типовой `Склады`. Это не карта магазинов.

След Форус/снэкс в **основной** Рознице (имена метаданных): справочник `снэксРегионы`, регистр `снэксВыгруженныеЧекиВTXT`. В основной УТ: `снэксСоответствиеПолейЗагрузкиДанныхИзВнешнегоИсточника`, `снэксСоответствиеНаименованияКолонокПолямЗагрузки`.

## 4. Планы обмена (имена, не маршруты)

Розница, среди прочих: `СинхронизацияДанныхЧерезУниверсальныйФормат`, `ОбменУправлениеНебольшойФирмойБухгалтерия30`, `ОбменСПодключаемымОборудованиемOffline`, `ОбменСУРМК`, `Полный`.

УТ, среди прочих: `СинхронизацияДанныхЧерезУниверсальныйФормат`, `ОбменУправлениеТорговлейРозница` (синоним «Розница, редакция 2.2»), `ОбменУправлениеТорговлейБухгалтерияПредприятия`, `ОбменСПодключаемымОборудованиемOffline`.

Какой план реально живой, по каким узлам и с каким расписанием — из метаданных **не** следует. Логов обмена на шаре нет (есть текстовый файл предупреждений загрузки УТ–УТ в папке финансиста — не разбирался как production log).

## 5. Расширения

Шесть XML-выгрузок расширений. Заказчик: работать **пока какие есть** — это рабочий набор на сейчас, не доказательство полного списка включённых в ИБ. Это не 14 расширений исходного анализа УТ.

Черновик паспортов: [extension-passport.draft.sanitized.json](extension-passport.draft.sanitized.json). Disposition везде `UNDECIDED` (не ставить KEEP/RETIRE). `active=true` значит «файл передан», а не «включён в боевой ИБ», пока конфигуратор не сверен.

| Файл | Имя расширения | Префикс | Назначение в XML |
|---|---|---|---|
| F.zip | `СНЭКС_Доработки` | `Дораб_` | обмен и заказы поставщикам |
| F-2.zip | `СНЭКС_ФункционалОбъединенияВидовЦенПоРегионам` | `снэкс_` | виды цен / регионы, vendor НПК ФОРУС |
| Forus-1.zip | `Форус_ОснованиеТоварнойНакладнойВозврат` | `ф_` | печать ТОРГ-12 |
| maxma.zip | `maxma` | `maxma_` | AddOn MAXMA / РМК |
| YT-1.zip | `СНЭКС_ДоработкаОбмена` | `Дор_` | универсальный формат обмена |
| YT-2.zip | `СНЭКС_ЗагрузкаДанныхИзВнешнегоИсточника` | `Загр_` | загрузка из внешнего источника |

В этих ZIP **нет** имён исходного анализа `ПомощникЗакупок`, `пр_НоменклатураРасширенныйПремиум`, `ХЛ_PNL`, `СФ_ДоработкиОбменаСОфлайнОборудованием`. Отсутствие в этой папке ≠ отсутствие в базе: **не** объявлять `ПомощникЗакупок` отсутствующим в ИБ.

## 6. Чего не хватает для D-47 / G1

| Нужно | Есть после ответов 1.1? |
|---|---|
| `Configuration.xml` Розницы | да, `ConfigFiles.zip` |
| Роль `/Розница` + `ConfigFiles.zip` | да, заказчик: центральная рабочая Розница 3.0.13.342 |
| `Configuration.xml` УТ | да, `YT.zip` |
| Роль `/УТ Новая` + `YT.zip` | да, заказчик: центральная УТ **пилота** (не «единственная боевая УТ навсегда») |
| Расширения | шесть переданных XML как рабочий набор; полнота списка ИБ **не** подтверждена |
| Прочие базы (legacy УТ, БП, узлы sn101…) | отложены заказчиком, не «их нет» |
| Данные НСИ (DT/XML элементов) | нет |
| БП | нет |
| GUID контрольного магазина | нет |
| hop / контрольный день | нет |

**G1 не открывается:** нет данных НСИ, нет БП, нет hop. Ответы 1.1 этого не меняют. Сценарий B (`dumpLagWeeks`) **не** включать: манифест D-47 по-прежнему неполный.

D-01 **сужен** (релиз УТ пилота `11.5.22.164` из XML + роль `UT_CENTRAL` пилота), **не закрыт** (`platformVersion`, полный перечень расширений ИБ, режим поддержки не сверены с конфигуратором). D-47 **сужен** (две выгрузки идентифицированы), **не закрыт**. Остальные D-02…D-46, D-48 без этих ответов не закрываются. Даты не назначались.

## 7. Что не делать

- не коммитить zip / xml / cf / cfe / dt / v8i;
- не грузить дампы в PROD;
- не заполнять `mdm-object-catalog` экземплярами;
- не объявлять disposition 14 расширений УТ;
- не считать план обмена «Розница 2.2» в УТ 11.5 доказательством текущего hop.

## 8. Индекс и MCP

Публичные индексы в Git: компактные JSON полных конфигураций + пообъектные JSON шести расширений. **Нет** тел BSL, **нет** массивов имён процедур, **нет** данных ИБ.

| Артефакт | Где |
|---|---|
| Компактный индекс Розницы | [retail-config-index.json](retail-config-index.json) |
| Компактный индекс УТ | [ut-config-index.json](ut-config-index.json) |
| Расширения | [indexes/](indexes/) |
| MCP Cursor | `.cursor/mcp.json` → сервер `snax-1c` (`scripts/snax_1c_mcp.py`) |

После `git pull` локально: Cursor → Settings → MCP → включить `snax-1c`. Инструменты: `list_1c_dumps`, `search_1c_metadata`, `get_1c_object` (плюс совместимые `search_retail_catalogs` / `get_retail_catalog` / `retail_dump_stats`). Облачный агент этот сервер к уже запущенной сессии не подключает.

Это не установка БСП в базу 1С и не загрузка XML в конфигуратор.

Пересобрать компактный индекс (zip только в `.local`):

```bash
python3 scripts/index_1c_xml_config_dump.py \
  --zip ".local/dumps/incoming/yandex-2026-08-27/Розница/ConfigFiles.zip" \
  --json-out docs/research/2026-08-27/retail-config-index.json \
  --intake-id a19e6c40-7b2d-4f11-8c55-2d9a0b47e801 \
  --source-file-name ConfigFiles.zip
```
