# List-file информационных баз · 27.08.2026

Статус: **получен, не коммитится**. Файл вида 1С `*.v8i` был в корне публичной папки. В Git его нет: в нём есть строки подключения (серверные `Connect=`). Паролей в файле не видно; хосты и имена информационных баз всё равно не публикуются.

Ниже — только **отображаемые имена** секций (то, что пользователь видит в списке баз). Это инвентарь ярлыков, не карта контуров D-47 и не доказательство, какие базы боевые.

| Отображаемое имя | Заметка |
|---|---|
| Розница | Есть отдельная XML-выгрузка в папке `/Розница` |
| Магазины | Ярлык в list-file; отдельной выгрузки с таким именем на шаре нет |
| Sn110 | Ярлык узла; выгрузки нет |
| SN109 | Ярлык узла; выгрузки нет |
| sn106 | Ярлык узла; выгрузки нет |
| sn105 | Ярлык узла; выгрузки нет |
| sn102 | Ярлык узла; выгрузки нет |
| sn101 | Ярлык узла; выгрузки нет |
| УТ | Есть XML-выгрузка в папке `/УТ Новая` |

Не делать: не копировать файл в репозиторий; не вставлять `Connect=` / хосты / `Ref=` в документы; не объявлять `sn101`… контрольными магазинами MDM.
