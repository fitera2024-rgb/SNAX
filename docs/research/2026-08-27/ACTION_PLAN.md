# Точные действия · после шары 27.08.2026

Программный план AP-0…AP-8 не заменяется: [TECHNICAL_ACTION_PLAN.md](../../TECHNICAL_ACTION_PLAN.md). Приём: [dump-intake-YANDEX-2026-08-27.md](dump-intake-YANDEX-2026-08-27.md). Чек-лист 26.08 (MCP каталогов): [../2026-08-26/ACTION_PLAN.md](../2026-08-26/ACTION_PLAN.md).

Открытые D-01…D-48 **не угадываются**. Релиз Розницы/УТ **прочитан из XML**. Заказчик письменно подтвердил роли двух переданных узлов (не гипотеза папки). Состав живых расширений ИБ и прочие базы **не** закрыты. G1 **не открывается**. `dumpLagWeeks` **не** включать из-за этих ответов.

## 0. Факт на сейчас

| Что | Статус |
|---|---|
| Шара | https://disk.yandex.ru/d/dfKzWDC27vtTFg |
| Розница `ConfigFiles.zip` | Полный XML, `Розница` `3.0.13.342`, 735 справочников = набор 26.08. Заказчик: **центральная рабочая Розница**. Контур `RETAIL_CENTRAL` подтверждён |
| УТ `YT.zip` | Полный XML, `УправлениеТорговлей` `11.5.22.164`. Заказчик: **центральная УТ пилота**. Контур `UT_CENTRAL` подтверждён. Это не объявление, что legacy УТ мертва |
| Расширения | 4 XML Розницы + 2 XML УТ. Заказчик: **пока какие есть** (рабочий набор на сейчас, не сверка конфигуратора) |
| Данные ИБ / DT / БП | Нет. Прочие базы (legacy УТ, БП, узлы sn101…) **отложены** заказчиком, не «их нет» |
| List-file `*.v8i` | Получен, не в Git; 9 отображаемых имён |
| D-01 | **Сужен**, не закрыт: релиз УТ пилота `11.5.22.164` из XML + роль узла. Открыто: `platformVersion`, полный перечень включённых расширений, режим поддержки не сверен отдельно |
| D-47 / G1 | D-47 **сужен** (две выгрузки идентифицированы), **не закрыт**. G1 не открывается: нет данных НСИ, нет БП, нет hop |

Две оси по-прежнему **не смешивать в одном PR**: сервис отдельно от разбора дампов.

## 1. Заказчик 1С

Передача вне Git (диск / SFTP / bucket), рядом имя + размер + SHA-256. **Не** присылать новый `*.v8i` со строками подключения.

### 1.1. Письменно — ответы получены (2026-08-27, буквально)

| № | Вопрос | Ответ заказчика |
|---|---|---|
| 1 | `/Розница` + `ConfigFiles.zip` — это центральная рабочая Розница 3.0.13.342? | **да** |
| 2 | `/УТ Новая` + `YT.zip` — это центральная УТ пилота? | **да** |
| 3 | Какие ещё базы выгружать (legacy УТ, БП, узлы sn101…)? | **это будем позже** |
| 4 | Полный список включённых расширений в конфигураторе Розницы и УТ | **пока какие есть** (шесть уже переданных XML: F, F-2, Forus-1, maxma, YT-1, YT-2) |

Не выводить сверх сказанного: legacy УТ не объявлять мёртвой; шесть XML — не доказательство полного списка включённых в ИБ; disposition остаётся `UNDECIDED`; `active=true` по-прежнему значит «файл передан».

### 1.2. Данные / БП / прочие базы — позже

Заказчик отложил. Исполнитель **не** требует эти файлы сейчас и **не** создаёт заглушки.

Когда заказчик вернётся к этому пункту (не назначена дата):

1. Остальные базы тем же комплектом XML, **если** они ещё рабочие: legacy УТ, БП, региональные Розницы `sn101`… — не считать их отсутствующими.
2. Остальные расширения как `*.cfe` или XML — **только если они реально есть** в ИБ. Не создавать файлы «для галочки». Имена вроде `ПомощникЗакупок` **не** объявлять отсутствующими в ИБ из-за этой папки.
3. Обезличенный срез **данных** на TEST (не PROD): `Организации`, `СтруктурныеЕдиницы` (Розница), `Склады` (УТ), `Кассы`/`КассыККМ`, плюс указание контрольного магазина.
4. Выгрузка конфигурации **БП** (хотя бы `Configuration.xml` + план обмена с УТ).
5. Не класть в ту же шару прайсы и выписки — для дампов заведите отдельную папку `dumps/`.

## 2. Исполнитель

| № | Делать | Не делать |
|---|---|---|
| 1 | Держать payload в `.local/`; манифест и индексы в Git | Коммитить ConfigFiles.zip / YT.zip / v8i |
| 2 | Опираться на эти две базы как на центральную Розницу и УТ **пилота** в разборе метаданных | Заполнять GUID в MDM; открывать G1 |
| 3 | Гипотеза магазина = `СтруктурныеЕдиницы` в Рознице (без элементов справочника) | Считать гипотезу VERIFIED |
| 4 | Паспорта 6 расширений как `UNDECIDED`; рабочий набор на сейчас | Auto KEEP / RETIRE; объявлять полный CFG-001 |
| 5 | Ось A сервиса на синтетике (другой PR) | Ждать полный D-47, чтобы merge WORK-005; включать `dumpLagWeeks` |
| 6 | Повторить карантин при следующем ZIP; тот же SHA = тот же `intakeId` | Второй intake на тот же SHA |

Локальный MCP: `snax-retail-xml` — индекс справочников 26.08; `snax-1c` — полные конфигурации 27.08 и расширения.

Включить `snax-1c` на этом компьютере:

1. `git pull` ветки с индексами.
2. Cursor → Settings → MCP → включить `snax-1c`.
3. Проверить `list_1c_dumps` (ожидаются `RETAIL_CONFIG`, `UT_NEW_CONFIG` и шесть `*_EXT_*`).
4. Не ждать, что облачный агент сам подключит MCP к уже запущенной сессии.

## 3. Критерии

| Цель | Готово | Ещё нет |
|---|---|---|
| Полный XML Розницы | `Configuration.xml`, релиз 3.0.13.342; заказчик: центральная рабочая | `platformVersion`; данные ИБ |
| Полный XML УТ | релиз 11.5.22.164; заказчик: центральная УТ **пилота** | `platformVersion`; данные ИБ; не доказано, что это единственная боевая УТ |
| Расширения | шесть переданных XML как рабочий набор | сверка включённых в конфигураторе; KEEP/RETIRE |
| D-01 | сужен: релиз УТ пилота из XML + роль узла | полный перечень расширений ИБ, режим поддержки, платформа |
| D-47 | сужен: две выгрузки идентифицированы | данные НСИ, БП, прочие базы (отложены), полнота расширений |
| G1 | — | данные НСИ, БП, hop; контрольный день |
