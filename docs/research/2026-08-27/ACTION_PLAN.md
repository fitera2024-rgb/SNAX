# Точные действия · после шары 27.08.2026

Программный план AP-0…AP-8 не заменяется: [TECHNICAL_ACTION_PLAN.md](../../TECHNICAL_ACTION_PLAN.md). Приём: [dump-intake-YANDEX-2026-08-27.md](dump-intake-YANDEX-2026-08-27.md). Чек-лист 26.08 (MCP каталогов): [../2026-08-26/ACTION_PLAN.md](../2026-08-26/ACTION_PLAN.md).

Открытые D-01…D-48 **не угадываются**. Релиз Розницы/УТ **прочитан из XML**, но контур узла и состав живых расширений УТ не подтверждены. G1 **не открывается**.

## 0. Факт на сейчас

| Что | Статус |
|---|---|
| Шара | https://disk.yandex.ru/d/dfKzWDC27vtTFg |
| Розница `ConfigFiles.zip` | Полный XML, `Розница` `3.0.13.342`, 735 справочников = набор 26.08 |
| УТ `YT.zip` | Полный XML, `УправлениеТорговлей` `11.5.22.164` |
| Расширения | 4 XML Розницы + 2 XML УТ |
| Данные ИБ / DT / БП | Нет |
| List-file `*.v8i` | Получен, не в Git; 9 отображаемых имён |
| D-47 / G1 | Не закрыты |

Две оси по-прежнему **не смешивать в одном PR**: сервис отдельно от разбора дампов.

## 1. Заказчик 1С — следующее конкретное действие

Сделать **в этом порядке**, передача вне Git (диск / SFTP / bucket), рядом имя + размер + SHA-256. **Не** присылать новый `*.v8i` со строками подключения.

### 1.1. Письменно, без файла

1. Папка `/Розница` / `ConfigFiles.zip` — это **центральная** рабочая Розница или узел? Как она называется в «О программе» рядом с `3.0.13.342`?
2. Папка `/УТ Новая` / `YT.zip` — это **центральная** УТ 11.5, с которой пойдёт пилот заказа?
3. Какие ещё рабочие базы надо выгрузить тем же комплектом (legacy УТ, БП, региональные Розницы `sn101`…)?
4. Полный список расширений **включённых в ИБ** (имя как в конфигураторе), отдельно для Розницы и УТ. Сверить с шестью уже переданными.

### 1.2. Недостающее для D-47

1. Остальные расширения УТ как `*.cfe` или XML: ожидаются имена вроде `ПомощникЗакупок`, `пр_НоменклатураРасширенныйПремиум`, `ХЛ_PNL` — **только если они реально есть**. Не создавать файлы «для галочки».
2. Обезличенный срез **данных** на TEST (не PROD): `Организации`, `СтруктурныеЕдиницы` (Розница), `Склады` (УТ), `Кассы`/`КассыККМ`, плюс указание контрольного магазина.
3. Выгрузка конфигурации **БП** (хотя бы `Configuration.xml` + план обмена с УТ).
4. Не класть в ту же шару прайсы и выписки — для дампов заведите отдельную папку `dumps/`.

## 2. Исполнитель

| № | Делать | Не делать |
|---|---|---|
| 1 | Держать payload в `.local/`; манифест и индексы в Git | Коммитить ConfigFiles.zip / YT.zip / v8i |
| 2 | Гипотеза магазина = `СтруктурныеЕдиницы` в Рознице | Заполнять GUID в MDM |
| 3 | Паспорта 6 расширений как `UNDECIDED` | Auto KEEP / RETIRE |
| 4 | Ось A сервиса на синтетике (другой PR) | Ждать полный D-47, чтобы merge WORK-005 |
| 5 | Повторить карантин при следующем ZIP; тот же SHA = тот же `intakeId` | Второй intake на тот же SHA |

Локальный MCP: `snax-retail-xml` — индекс справочников 26.08; `snax-1c` — расширения и объекты 27.08.

## 3. Критерии

| Цель | Готово | Ещё нет |
|---|---|---|
| Полный XML Розницы | `Configuration.xml`, релиз 3.0.13.342 | Подтверждение, что это центральный узел |
| Полный XML УТ | релиз 11.5.22.164 | Подтверждение центральной УТ |
| D-47 | — | данные НСИ, БП, полный список расширений УТ, роли узлов |
| G1 | — | контрольный день по hop |
